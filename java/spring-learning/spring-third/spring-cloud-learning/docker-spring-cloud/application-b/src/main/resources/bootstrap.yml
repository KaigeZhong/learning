spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    serviceUrl:
#eureka的地址为docker容器名
      defaultZone: http://eureka-server:8761/eureka/